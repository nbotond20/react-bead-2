import CardContainer from '../utils/CardContainer';

const Tasks = () => {
    return (
        <CardContainer>
            <h1 style={{ textAlign: 'center' }}>Tasks</h1>
        </CardContainer>
    );
};

export default Tasks;
